language: node_js

node_js:
    - node

services: 
    - docker

before_script:
    - docker-compose build
    - docker-compose up -d db frontend-test backend-test

script:
    - cd ./backend
    - npm run test
    - cd ..
    - cd ./frontend
    - npm run end-to-end-test